export class PR {
  constructor(id, user_id, exercise_id, weight, reps, date, created_at) {
    this.id = id;
    this.user_id = user_id;
    this.exercise_id = exercise_id;
    this.weight = weight;
    this.reps = reps;
    this.date = date;
    this.created_at = created_at;
  }
}
