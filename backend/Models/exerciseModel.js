export class Exercise {
  constructor(id, name, category, is_default, created_at) {
    this.id = id;
    this.name = name;
    this.category = category;
    this.is_default = is_default;
    this.created_at = created_at;
  }
}
